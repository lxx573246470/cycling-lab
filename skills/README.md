# Project Skills

本目录记录 `cycling-lab` 项目内约定的 Codex skill。可读版本在 `skills/cycling-lab/SKILL.md`，Codex 项目版本同步放在 `.codex/skills/cycling-lab/SKILL.md`。

- `cycling-lab/`: 本仓库的训练数据、周计划、FIT 分析和周复盘工作流。

全局 FIT 解析能力来自本机已安装的 `cycling-fit-analysis` skill。本仓库的 `scripts/analyze_fit.py` 会调用该全局 skill 脚本。
